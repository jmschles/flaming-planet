<h2>Edit donation</h2>

<%= render "form.html", changeset: @changeset, button_text: "Submit",
                        action: manage_donation_path(@conn, :update, @donation) %>

<%= link "Back", to: manage_donation_path(@conn, :index) %>
